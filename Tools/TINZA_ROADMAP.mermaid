%% TINZA — THE ROAD TO "JUST ADD RECIPES"  (15 Jul 2026, with session estimates)
%% Prize: a new recipe = pure data, no code. Session counts are rough, solo late-night pace.
flowchart TB
    START(["WHERE WE ARE — ~10 sections, each doing the same jobs a different way"])
    START --> P1

    P1["1 · DRAW THE MAP  ·  ~1 session<br/>Census every copy of portion, cost, search, diet & the recipe page. No building.<br/>✅ DONE = one document listing every copy."]
    P1 --> P2["2 · AGREE THE ONE FORM (recipe contract)  ·  ~1–2 sessions<br/>The single structured shape every recipe fills in — forever.<br/>★ category leaders solve THIS first · future-proof export/import<br/>✅ DONE = the form written down + ruled."]
    P2 --> P3["3 · BUILD THE ONE ENGINE (spine in core.js)  ·  ~4–5 sessions<br/>Shared code turns a filled form into a finished page:<br/>portion · cost (19→1) · search (one index, finishes MF98) · diet · opener.<br/>Built with shims — screen unchanged, nothing breaks.<br/>✅ DONE = engine works, app looks identical."]
    P3 --> P4["4 · MOVE EVERY SECTION ONTO THE ENGINE  ·  ~10–12 sessions (≈1 per section)<br/>Braai (proof) → World Kitchen → Feed My Family → Spice → Events → Health → Kiddies → Budget → Finger.<br/>Each visit routes ALL jobs at once · tablet-verify · ratchet so it can't drift.<br/>✅ DONE = every section on the engine · zero copies left."]
    P4 --> P5["5 · FRONT DOOR ON THE SAME ENGINE  ·  ~2–3 sessions<br/>Home page built from the same shared cards & shelves · shelves query live recipes (MF117).<br/>★ offline app-shell = instant loads on spotty SA connections<br/>✅ DONE = home page same-built, not a one-off."]
    P5 --> P6["6 · LOCK IT + PROVE THE PROMISE  ·  ~1–2 sessions<br/>Doctor guards every job. Final test: add ONE recipe as pure data → it appears fully dressed, zero code.<br/>✅ DONE = adding a recipe needs no building."]
    P6 --> PRIZE(["THE PRIZE — the app is SAME everywhere.<br/>From here, building = adding recipes. No code.<br/>CONVERGENCE TOTAL ≈ 19–25 sessions"])

    PRIZE --> LAUNCH["LAUNCH PLUMBING  ·  ~5–7 sessions  ·  runs in PARALLEL, not after<br/>MF57 cache (hand-written versioned SW, no build) · save favourites & plans · PayFast R90 ·<br/>AI cost cap · hide tier switch · home polish"]
    LAUNCH --> LIVE(["LAUNCH-READY  ·  not every section must be converged first —<br/>the app works today; convergence is what makes it last"])
