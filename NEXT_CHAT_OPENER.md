# PASTE THIS AT THE TOP OF THE NEXT CHAT

Tinza session. Purpose: **SAMENESS — Law 6, build the ONE thing they call.**

Start by cloning and measuring, don't ask me for code:
`git clone --depth 1 https://github.com/tinavdw/tinza.git` then `node tinza-census.js`.

**Last session closed (do not reopen):** §11 ROOM/SHELF/TOOL ruling filed · R50→R90 sweep, 21 strings across 9 section files, pushed and verified by me on live in 7 rooms · census check 25 gained rung ⑦ (`PRO_PRICE = 90`, one place).

**This session, in this order:**

1. **`goBack()` — one line.** Census 8: it dumps me on Home from any root screen. `core.js` step (4) ignores `S.searchPrevScreen`, which braai.js, spice.js and liveSearch() already set. One line in `goBack()`, not one fix per room.
2. **`shareList()`** — census 9: 21 WhatsApp copies that have already drifted into **five different labels** and two button styles. ("straight to" vs "directly to" — nobody chose either.)
3. **`TINZA_PRICE` (MF91)** — census 9: the price is still written 21 times. Last session fixed the *value*; this fixes the *duplication*.
4. **`openSectionSearch(scope)` (MF95)** — census 9 + 6: braai.js and spice.js have search *pills that navigate away*. Ruling R1 says the search pill never navigates.
5. **`sectionHeader()` gains `sub:true`** — the build side of §11. Kills barplanner.js's hand-rolled header at :214. Note budget.js and tinyTummies.js have **no `sectionHeader()` at all**.

**Every one of these gets a census assertion, or it rots.** The R50→R90 sweep sat in a queue for four weeks and did nothing. A queue is a memory; only a watcher is a mechanism.

**Three things I still have to rule — ask me, don't infer (§2.3):**

- The §11 seam: **I've Got R100 · I Have Chicken… · Just Feed Me** take an input like a TOOL but return a scrollable recipe list like a SHELF. Do they get a search box? (Proposal on the table: input state = tool, result state = shelf.)
- "§11" now names three different things in TINZA_RULINGS.md — the LOCALE ranking clause (:295), the store (:963), and the tool ruling (:662).
- `core.js:656` — `_warm` is a hard-coded allow-list of 10 screens. **Home isn't on it**, nor are any of the six Smart Features. The front door is the last unmigrated room. When do we move the line?

**Standing rules:** you read the repo at HEAD, my eyes on tinza.netlify.app close every bug (Law 2). You can't push — I push via GitHub Desktop, one commit, one push. `node --check` before handback. End with a flowchart + memory update.
