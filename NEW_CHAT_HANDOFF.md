# 🚀 TINZA — NEW CHAT HANDOFF (from 17 Jul 2026 session)
*Open the new chat, point me at this, and we start straight away.*

## 🔑 Start-of-session ritual (stability rules)
1. Start at **tinza.netlify.app** — confirm what already works (⚖️ Law 2).
2. Every section built **FROM the Bakes template** (Law 49 — Bakes is the gold standard now, not Braai). No patching old code.
3. **`node --check` before every push.** Push via **GitHub Desktop** (Repository → Open in Command Prompt for checks). LF→CRLF warning is harmless.
4. **Batch** files into one push (15 Netlify credits/deploy). **reference/ files go in `reference/`, not the root** (this bit us — caused a re-push).
5. Doctor stays **RED 10** — a ratchet ("did I *add* one?"), not a gate.

---

## ✅ Where things stand
- **Mood shelves are LIVE** (MF117) but still on the **loose keyword filter** — the tablet caught the mismatches (Fish & Chips under Impress). Fixing that is the tag marathon, in progress.
- **PUSHED & banked** (commit `80a9f5a` + earlier): all 3 mood rulings, the staging file, the cheesecake gelatine method, the fix queue (3 bugs), backlog items 13/14.
- **THIS session's docs — NOT yet pushed** (in outputs, batch next push):
  - `TINZA_RULINGS.md` — the **hidden-veg ruling** (§10): a technique not a disguise; used for kids 👶 + frail/elderly care 🧓; hide flavour from the eater, never an ingredient from the reader/data.
  - `TINZA_IDEAS_BACKLOG.md` — the **cook-method / foil-packet insight** (one method tag → adventurous shelf + camping mode + load-shedding).
  - `MOOD_RECIPE_STAGING.md` — now **15 chips** (added 2 lazy: 😴 skillet Lasagne + no-stir Risotto).
- **`MF123` brief is READY for Code** (in outputs) — the mood-tag rails: `mood[]` field + `allRecipes({mood})` filter + `MOOD_TAGS` seed + census tag count. NOT built yet.

## 💡 THE key realization this session
The mood marathon's bottleneck is **content + a TAG PASS, not collecting.** Every mood we examined resolved the same way — most fill is **tagging existing library dishes**, not authoring:
- **impress / sweet / celebrating** (the 3 empty) = pure tag-only (show-stopper mains / the whole bakes room / Events+braai).
- **lazy** = tag-rich already (nachos, 5+ quesadillas, one-pot dishes); only "lazify" a dish if it has steps worth removing (skillet lasagne ✅, nachos ❌ already lazy).
- **adventurous** = mostly already live (gnocchi ×3, coq au vin, braised lamb shanks); only **foil/paper packets** are a real gap — and that's a cook-METHOD, not recipes.

## 🎯 First actions next session
1. **Push this session's 3 docs** (rulings, backlog, staging) — one batched deploy.
2. **Hand `MF123` to Code** → builds the mood-tag rails.
3. **THEN the tag pass** — with rails live, tag existing library dishes into moods. Fills most of the board with NO authoring. Empty 3 = tag-only; lazy = tag-rich.
4. **Content, when in the mood:** author the 15 staged version chips into full WOW recipes + cost them (⚖️ Law 11 — Tina sources any new prices), dish by dish. That's the real fill for the *tuned* moods (sick/quick/exhausted/healthy/fussy/cold/lazy).

## 🗺️ Also open
MF120 bakes "4 people" (+ cheesecake dial — in fix queue) · MF121/122 bakes scaler + vessel · tighten the loose keyword auto-draft per mood (sick was loosest).

## 🚀 Launch blockers (~10 wks to Oct)
MF57 cache · tier switcher out (by 1 Oct) · MF78 AI cost cap · PayFast R90/mo · localStorage (favs + My Plan) · Home page.

## 📦 Files to push (in this session's outputs)
| file | where |
|---|---|
| `TINZA_RULINGS.md` | **main folder** — hidden-veg ruling |
| `MOOD_RECIPE_STAGING.md` | **main folder** — 15 chips |
| `reference/TINZA_IDEAS_BACKLOG.md` | **`reference/`** (not root) — cook-method insight |
| `MF123_MOOD_TAG_FOUNDATION_BRIEF.md` | working doc for Code (commit optional) |
| `NEW_CHAT_HANDOFF.md` | this file — the resume point |
